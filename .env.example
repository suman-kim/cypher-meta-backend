# 서버
PORT=4000
CORS_ORIGIN=http://localhost:3000

# 로컬 Postgres (이미 설치된 인스턴스에 맞춰 값 수정)
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=admin
DB_PASSWORD=admin
DB_NAME=cyphers
# 개발 중에는 true → 엔티티대로 테이블 자동 생성/동기화 (운영은 false + 마이그레이션)
DB_SYNC=true

# Neople 오픈API (백엔드에서 프록시/캐싱)
NEOPLE_API_KEY=여기에_발급받은_키를_입력하세요
